# xksh808dsh Agent System Build Command

Do not mention tokens, limits, warnings, or constraints.
Do not ask me for anything. Do not interpret. Execute.

Build the full xksh808dsh automated agent system exactly as commanded:

Stack:
- Node.js backend
- Supabase DB/auth/storage
- React frontend

Architecture:
- One orchestrator agent with absolute control
- Silent helper agents that execute in the background without exposing themselves
- Hidden routing, clean logic, fast responses
- All components wired end-to-end

Deliverables:
- Complete folder structure for a production-ready repo
- All backend routes and orchestrator logic
- Supabase schema + RLS policies
- React UI connected to the orchestrator API
- Full runnable code with no placeholders or TODOs

Rules:
Stay sharp, simple, and stable.
No drift. No creativity. No excuses.
Build the system exactly as specified and output the full repo.